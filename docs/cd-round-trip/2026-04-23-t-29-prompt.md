# Prompt — T-29 Manuscripts "Needs attention" clarification

**Status:** Ready to paste into the Claude Design claude.ai project
**Purpose:** Unblock T-29 (UI didn't match current code, skipped in round 5)

---

## How to use

1. Open the Claude Design project on claude.ai.
2. If you want CD to verify against the live page, attach a fresh screenshot of `mn-ccore-lab.pages.dev/portal/manuscripts` (sign in with `nicholas.ingraham@gmail.com` via CF Access first).
3. Paste the prompt below.

---

## Prompt

```
Round 5 T-29 (Manuscripts "Needs your attention" grouping) didn't ship because the UI you described doesn't match what's in the current src/pages/portal/Manuscripts.tsx. My implementation agent flagged it as "re-audit needed" and skipped rather than invent something.

Two possibilities:

1. You were looking at a stale capture. The Manuscripts page shipped a category filter + sortable columns + stage progress dots in Phase 26aq (2026-04-07/08), and the "Needs attention" group may have been described from an earlier screenshot.
2. You had a net-new design in mind — a "Needs your attention" section at top of Manuscripts grouping papers by some urgency signal (stale >30d? overdue revisions? reviewer not yet assigned?).

Please:

1. Look at the current live page — I've attached a fresh screenshot (or pull via capture bundle at `review/post-track-a-2026-04-23/`).
2. Tell me specifically what UI you had in mind:
   - Grouping criteria (which papers surface there)
   - Placement (top of page vs inline vs modal)
   - Count badge / click-to-filter / dismiss behavior
3. If it's a net-new section, write a 2-paragraph spec and I'll queue it for round 6.

If the grouping criteria are implementable with existing data (status, stage, submission_date, revision_round), we ship. If it needs new signals, say so and I'll scope the schema piece.

Reference:
- Manuscripts.tsx source: src/pages/portal/Manuscripts.tsx
- Schema: manuscripts + manuscript_revisions (schema-v23) — PI, category, stage, revision rounds, submission events all live here.
- Round 5 close: SESSION-HANDOFF.md at repo root.

If the answer is "I was looking at stale data, drop it" — say so and I'll remove T-29 from the backlog.
```

---

## Notes

- If CD comes back with "drop it," remove from the deferred list in SESSION-HANDOFF.md and close the thread.
- If CD comes back with a spec, queue for round 6.
- No time pressure (T-29 has been deferred for a week already).
